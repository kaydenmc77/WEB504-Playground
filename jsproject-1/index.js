// Gets module element from HTML and sets it as an immutable variable
const module = document.getElementById('module')
// Gets module closing button from HTML and sets it as an immutable variable
const moduleClose = document.getElementById('module-close')
// Gets bookmark form from HTML and sets it as an immutable variable
const bookmarkForm = document.getElementById('bookmark-form')
// Gets website name element from HTML and sets it as an immutable variable
const websiteNameEl = document.getElementById('website-name')
// Gets website url element from HTML and sets it as an immutable variable
const websiteUrlEl = document.getElementById('website-url')
// Gets bookmarks container from HTML and sets it as an immutable variable
const bookmarksContainer = document.getElementById('bookmarks-container')